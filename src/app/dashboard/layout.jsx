export const metadata = {
    title: 'Dashboard',
    description: 'Dashboard de la administracion',
}

export default function DashboardLayout({ children }) {
    return (
        <>
            <header>2</header>
            <main>{children}</main>
            <footer>
                2
            </footer>
        </>
    )
}
